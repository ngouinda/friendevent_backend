pipeline {
    agent any

    stages {
        stage('CI') {
            steps {
                git branch: 'main', url: 'https://github.com/fredericBui/friendevent_backend.git'
            }
        }
    }
}
